import * as React from "react";
import type { PageProps } from "gatsby"
import "../styles/index.css"

const Portfolio: React.FC<PageProps> = () => {
    return (
        <div className="portfolio">
            <h1>ITS MEE from portfolio</h1>
        </div>
    )
};

export default Portfolio;