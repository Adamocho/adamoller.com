import * as React from "react";
import type { PageProps } from "gatsby"
import "../styles/index.css"

const Contact: React.FC<PageProps> = () => {
    return (
        <div className="contact">
            <h1>ITS MEE from contact</h1>
        </div>
    )
};

export default Contact;