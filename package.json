{
  "name": "personal-website",
  "version": "1.0.0",
  "private": true,
  "description": "personal-website",
  "author": "Adamocho",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "start": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "typecheck": "tsc --noEmit",
    "deploy": "gatsby build && gh-pages -d public -b site-render"
  },
  "dependencies": {
    "gatsby": "^5.14.1",
    "gatsby-omni-font-loader": "^2.0.2",
    "react": "^18.0.0"
  }
}
